import { LanguageSwitch } from "@/features/i18n/switch-language";
import { AppLayout } from "@/widgets/app-layout";

// Placeholder: Task 5 fills the property detail in (the title becomes the address).
export function PropertyPage() {
  return <AppLayout title="…" actions={<LanguageSwitch />}>{null}</AppLayout>;
}
